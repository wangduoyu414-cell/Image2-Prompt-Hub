# GPT Image 2 高价值案例聚合与持续同步平台设计

> 文档版本：v1.8
> 更新日期：2026-08-11
> 文档状态：七来源内部接入、审核、Publication/API/Web v2 和发布运营入口已完成；真实公开目录仍为 0

## 1. 项目定位

本项目不是通用提示词网站，也不是提示词工具、Agent 或工作流项目的聚合平台。

本项目只解决一个明确问题：

> 固定一批高价值案例数量充足、能够稳定提取完整 Prompt 与对应图片的开源项目，持续同步其内容，统一整理、分类、去重和审核后，通过网页提供图片浏览、Prompt 查看和一键复制。

网站只是内容消费端，系统核心是可长期运行的案例内容供应链。

完整数据流：

~~~text
通过审核的案例项目
→ Git 镜像与固定 Commit 快照
→ 项目专用 Source Adapter
→ Prompt 与图片强绑定
→ 统一 Generation Example
→ 图片资产处理
→ 跨项目去重与分类
→ 权利和质量发布门
→ 版本化发布
→ 网页展示、搜索和复制
~~~

## 2. 目标、范围与非目标

### 2.1 产品目标

系统应当做到：

- 稳定保存所有正式来源的当前内容和已处理历史版本。
- 从不同仓库结构中提取完整 Prompt、展示图、参考图和来源信息。
- 保证每张输出图片能追溯到与其对应的确切 Prompt。
- 将不同来源统一成一致的数据格式，但永远保留原始内容。
- 识别镜像、重复案例、翻译版本和近似变体。
- 对真实存量进行多维分类，而不是绑定死某个业务场景。
- 根据来源、权利、完整度和配对证据决定是否公开。
- 自动检测仓库更新，并安全处理新增、修改、删除和结构变化。
- 为网页提供稳定的图片、分类、搜索、详情和复制接口。

### 2.2 内容范围

只收录符合以下条件的案例型项目：

- 包含数量可观的真实案例。
- 每个案例具有完整或基本完整的 Prompt。
- 存在与 Prompt 对应的输出图片。
- 内容可以从 Git 仓库或稳定引用中提取。
- 来源身份、仓库版本和原始位置可以保存。
- 项目不是另一个来源的无差别重复镜像，或其镜像身份能够明确标记。
- 案例与 GPT Image 2 的关系能够逐条记录；混合模型项目必须能区分每个案例的模型声明或未知状态。

项目名称不作为收录依据：只要其真实内容满足案例来源标准，就按“案例项目”审核；反之，只有 Prompt 方法、工作流或执行代码而缺少大量图文案例的项目，不进入系统。

### 2.3 非目标

首版不负责：

- 聚合或安装提示词工具、Agent、MCP 和工作流。
- 执行外部仓库代码、安装其依赖或运行其构建脚本。
- 自动生成新图片来替代上游原始效果图。
- 将 AI 改写、翻译或模板化内容冒充上游原文。
- 无限制采集所有相关 GitHub 仓库。
- 默认公开所有已采集内容。
- 首版建设 Kubernetes、多套微服务或独立搜索集群。
- 将来源仓库许可证自动理解为覆盖其中所有第三方 Prompt 和图片。

## 3. 核心原则

### 3.1 固定优质来源，而不是追求来源数量

正式来源应少而可靠。项目能否成为长期来源，取决于内容密度、图文配对能力、结构稳定性、来源证据和权利可管理性，而不是 Star、仓库更新时间或主观综合评分。

### 3.2 全量进入内部库存不等于全量公开

通过来源审核的项目，其可提取内容可以完整进入内部 Source Inventory。

公开目录只包含同时满足以下条件的案例：

- Prompt 与图片配对明确。
- 原始来源可以追溯。
- 图片可正常获取和展示。
- 权利策略允许相应展示方式。
- 内容结构和质量达到发布要求。

### 3.3 原始事实与派生内容分离

必须分别保存：

- 上游原始 Prompt。
- 清理格式后的等义文本。
- 独立翻译。
- 可复用模板。
- 自动分类和分析结果。

任何派生内容都不得覆盖原始内容。

### 3.4 图片必须与确切 Prompt 强绑定

系统不能只在案例层保存一组 Prompt 和一组图片。每张输出图必须通过 Generation Example 绑定到确切 Prompt、输入图片、模型声明和参数声明。

### 3.5 失败时保留上一版

一次同步或解析失败不能导致已发布案例被误删。新版本只有通过完整校验后才能原子替换上一发布版本。

## 4. 来源准入与长期管理

### 4.1 来源准入维度

每个候选项目必须完成一次可重复的来源审计。

| 维度 | 必须检查的事实 |
| --- | --- |
| 内容价值 | 有效案例数量、Prompt 完整度、图片质量、案例多样性 |
| 可提取性 | Prompt 与图片的组织形式、自动配对率、是否依赖登录或动态页面 |
| 稳定性 | 仓库是否可克隆、图片是否可获取、结构是否频繁变化、是否使用 Git LFS |
| 维护性 | 是否存在真实内容更新、更新是否只是时间戳或 README 自动刷新 |
| 独立性 | 是否为原始项目、镜像、备份、翻译、衍生或聚合来源 |
| 来源证据 | 是否有作者、原帖、项目说明、模型和参数等证据 |
| 权利状态 | 仓库、Prompt、图片是否有独立许可或使用说明 |
| 接入成本 | 是否能编写确定性 Adapter、是否需要大量人工图文配对 |

聚合项目只有在能够保留每条案例的原始作者、原始链接和图文对应关系时，才有资格成为正式来源。

### 4.2 初始准入阈值

以下阈值是首版运营规则，可以在真实审计后调整：

- 常规正式来源建议至少包含 50 个有效图文案例。
- 垂直但独特的来源可以降低到 20 个有效案例。
- 自动图文配对率原则上不低于 90%。
- 关键字段无法提取或大量图片失效的项目不得直接进入 active。
- 镜像项目不重复产生前台内容。
- 需要执行仓库代码才能获得内容的项目不得接入。
- 权利不清不阻止进入内部库存，但阻止自动公开图片。

“有效案例”至少包含一段可复制的原始 Prompt 和一张能够明确配对的输出图片。

### 4.3 来源生命周期

每个来源必须处于以下状态之一：

| 状态 | 含义 |
| --- | --- |
| candidate | 已发现，尚未完成审计 |
| probation | 已完成初审，正在验证 Adapter 和配对质量 |
| active | 正式长期同步和处理 |
| paused | 暂停同步，保留已有数据 |
| retired | 不再更新，但保留历史内容 |
| blocked | 因权利、安全或内容问题禁止使用 |

来源名单必须版本化。新增、暂停、替换或移除来源都要记录原因和时间。

### 4.4 来源家族

每个来源还需要标记其内容角色：

- canonical：原始或主要维护来源。
- mirror：内容镜像。
- backup：灾备或历史备份。
- translation：翻译版本。
- derived：经过改编的衍生项目。
- aggregator：聚合多个上游来源。

镜像和备份可以保留版本与血缘，但默认使用 provenance_only，不重复创建公开案例。

### 4.5 Source Registry

所有来源使用 Git 管理的 YAML 注册表，不将仓库信息写死在程序中。

~~~yaml
registry_version: 1

sources:
  - id: example-source
    name: Example Prompt Gallery
    status: probation

    repository:
      provider: github
      url: https://github.com/example/example
      repository_id: "123456789"
      default_branch: main

    family:
      family_id: example-family
      role: canonical
      canonical_source_id: example-source

    sync:
      enabled: true
      cadence: 6h
      clone_mode: mirror_full
      lfs_mode: current_revision
      submodules: disabled

    content:
      adapter: example_gallery
      adapter_version: 1
      model_scope: mixed
      include:
        - "data/**/*.json"
        - "images/**/*"
      exclude:
        - ".github/**/*"

    admission:
      minimum_valid_cases: 50
      minimum_pair_rate: 0.90
      expected_case_count: 120
      maximum_count_drop_rate: 0.20

    rights:
      repository_license: unknown
      prompt_policy: review_required
      asset_policy: review_required

    publication:
      ingestion_policy: full
      auto_publish: false
~~~

## 5. 总体架构

### 5.1 四层数据结构

#### Source Layer：来源层

保存上游不可改写的事实：

- Source Project
- Source Revision
- Source File
- Source Asset Reference

这一层回答内容来自哪个仓库、哪个 Commit、哪个文件和哪个路径。

#### Evidence Layer：案例证据层

保存解析得到的原始案例和图文对应关系：

- Source Case Version
- Prompt Record
- Asset
- Generation Example
- Pairing Evidence

这一层回答哪段 Prompt、哪些输入图、哪些参数和哪些输出图属于同一次案例展示。

#### Canonical Layer：标准内容层

统一不同来源的表示，但不破坏来源证据：

- Canonical Case
- Canonical Membership
- Duplicate Cluster
- Taxonomy Assignment
- Derived Prompt Variant
- Field Provenance

这一层回答哪些来源案例是镜像、重复、翻译或变体，以及前台应如何统一展示。

#### Publication Layer：发布层

决定用户当前可以看到什么：

- Publication Version
- Visibility
- Rights Decision
- Quality Decision
- Search Document
- Featured State

### 5.2 模块划分

首版采用模块化单体，不拆成大量独立服务：

~~~text
Source Manager
├── 来源注册
├── Git 镜像
└── 更新检测

Ingestion Worker
├── Commit 快照
├── Source Adapter
├── 图文配对
├── 图片处理
└── Staging 校验

Content Core
├── 统一数据模型
├── 去重与来源血缘
├── 分类
├── 权利与质量门
└── 发布版本

Web Application
├── 公共网站
├── API
└── 管理后台
~~~

## 6. 核心数据模型

### 6.1 Generation Example 是最小可信内容单元

Generation Example 必须表达一组完整关系：

~~~text
确切原始 Prompt
+ 输入或参考图片及其角色
+ 上游声明的模型和参数
+ 对应输出图片
+ 来源 Commit 和文件位置
+ 图文配对证据
~~~

同一 Prompt 在不同模型、参数、时间或输入图下产生的结果，应保存为不同 Generation Example。

同一案例可以包含多个 Generation Example，但不能丢失各自的生成上下文。

### 6.2 建议 Schema

~~~json
{
  "id": "gen_01JEXAMPLE",
  "source": {
    "project_id": "example-source",
    "revision_sha": "abc123",
    "source_case_version_id": "source_case_version_01JEXAMPLE",
    "case_native_id": "case-001",
    "source_path": "gallery/case-001.json",
    "source_url": "https://github.com/example/example/blob/abc123/gallery/case-001.json"
  },
  "title_raw": "Luxury Product Poster",
  "prompt": {
    "record_id": "prompt_01JEXAMPLE",
    "variant_type": "original",
    "language": "en",
    "text": "Create a luxury product poster..."
  },
  "inputs": [
    {
      "asset_id": "asset_input_01",
      "role": "product_reference"
    }
  ],
  "outputs": [
    {
      "asset_id": "asset_output_01",
      "role": "output_primary"
    }
  ],
  "generation_claim": {
    "model_raw": "gpt-image-2",
    "model_evidence": "source_claimed",
    "parameters_raw": {
      "size": "1024x1536"
    }
  },
  "pairing": {
    "method": "explicit_structured_reference",
    "status": "strong",
    "confidence": 1.0,
    "evidence": [
      "prompt and image share the same source case id"
    ]
  },
  "provenance": {
    "author_raw": "@example",
    "original_post_url": null
  },
  "rights": {
    "prompt_rights_record_id": "rights_prompt_01",
    "asset_rights_record_ids": [
      "rights_asset_01"
    ]
  }
}
~~~

### 6.3 数据不变量

系统必须保证：

- 原始 Prompt 一经写入对应 Source Revision 就不可修改。
- 清洗、翻译和模板化必须产生独立记录。
- 每个公开 Generation Example 至少有一个输出图片。
- 每个输出图片必须明确关联一个 Generation Example。
- 未知模型或参数保存为 unknown 或 null，不得自动猜测。
- 上游声称的模型和参数保存为 source claim，不自动视为官方事实。
- 无法确认模型的案例不能被描述为“已由 GPT Image 2 生成”，只能标记为模型未知或兼容性未验证。
- 每个资产必须保存来源位置和内容哈希。
- Canonical 合并不能删除 Source Case 和 Generation Example。

### 6.4 核心数据库实体

首版需要的核心表：

~~~text
source_projects
source_families
source_revisions
source_files
source_sync_runs
source_adapter_runs
source_parse_errors

source_cases
source_case_versions
prompt_records

assets
asset_sources
generation_examples
generation_inputs
generation_outputs
pairing_evidence

canonical_cases
canonical_memberships
duplicate_clusters
duplicate_decisions

taxonomy_nodes
case_taxonomy
classification_runs

rights_records
publication_versions
review_events
takedown_requests

pipeline_jobs
outbox_events
dead_letter_jobs
~~~

## 7. Source Adapter 规范

### 7.1 Adapter 的职责

Adapter 只负责提取上游事实：

- 案例身份。
- 标题和原始 Prompt。
- 图片路径、外部 URL 和图片角色。
- Prompt 与图片之间的结构证据。
- 上游作者、链接、标签、模型和参数声明。

Adapter 不负责：

- 改写 Prompt。
- 决定最终分类。
- 判断案例是否优质。
- 合并跨项目重复。
- 决定内容是否公开。

### 7.2 Adapter 类型

基础 Adapter 可以复用通用能力：

~~~text
BaseSourceAdapter
├── StructuredJsonAdapter
├── MarkdownSectionAdapter
├── MarkdownGalleryAdapter
├── MarkdownTableAdapter
├── StaticSiteDataAdapter
└── GenericDiscoveryAdapter
~~~

正式来源仍应拥有项目专用 Adapter。GenericDiscoveryAdapter 只能生成候选记录，不能直接自动发布。

### 7.3 图文配对等级

| 配对方式 | 示例 | 发布策略 |
| --- | --- | --- |
| explicit_structured_reference | JSON 中图片和 Prompt 使用同一 Case ID | 可进入自动质量门 |
| explicit_markdown_block | 同一明确案例区块内包含 Prompt 和图片 | 可进入自动质量门 |
| stable_native_mapping | Prompt 文件名与图片目录存在稳定映射 | 可进入自动质量门 |
| inferred_local_order | 只能根据相邻顺序推断 | 内部库存，默认人工审核 |
| ambiguous | 一条 Prompt 对应多图关系不明 | 不公开 |

配对置信度只是辅助证据，不能替代配对方法和原始位置记录。

### 7.4 Adapter 测试

每个 active 来源必须保存固定 Commit 的测试夹具：

~~~text
fixtures/adapters/{source_id}/{commit_sha}/
├── source-files/
├── expected-cases.json
└── expected-metrics.json
~~~

CI 至少验证：

- 案例总数。
- 有效 Prompt 数。
- 输出图片数。
- 图文配对率。
- 空 Prompt 数。
- 孤立图片数。
- Schema 合法性。
- 关键案例仍然存在。
- 重复执行结果一致。

## 8. Git 镜像与持续同步

### 8.1 镜像策略

正式来源使用持久 Git 镜像，解析时创建临时只读工作区：

~~~text
/data/git-mirrors/{source_id}.git
/data/worktrees/{run_id}/{source_id}
/data/cache
~~~

默认规则：

- 不执行 Git Hook。
- 不自动执行子模块。
- 不安装依赖。
- 不运行构建脚本。
- 不将生产密钥挂载到解析容器。
- Git LFS 只获取当前待解析版本需要的对象。

每次成功处理的 Commit 建立内部保留引用，防止上游 Force Push、删除分支或删除仓库后历史证据丢失。

### 8.2 更新检测

active 来源按照注册表配置定时检测远端默认分支 SHA。

建议初始频率：

- 高频正式来源：6 小时。
- 普通正式来源：12 至 24 小时。
- paused 或 retired 来源：每周或停止检测。

系统应使用退避和随机抖动，避免所有来源同时请求 GitHub。

### 8.3 增量同步策略

仓库 Commit 变化后：

~~~text
拉取新 Commit
→ 创建 Source Revision
→ 全量重新解析该仓库当前版本的案例元数据
→ 与上一个解析版本做集合差异
→ 图片按 Git Blob Hash 或 SHA-256 增量处理
→ 仅重跑变化案例的分类和索引
→ 校验通过后原子发布
~~~

不根据单个 Markdown diff 局部打补丁，因为标题层级、图片顺序或公共变量变化可能影响多个案例。

### 8.4 幂等与原子发布

任务唯一键至少包括：

~~~text
sync:{source_id}:{commit_sha}
parse:{source_id}:{commit_sha}:{adapter_version}
asset:{sha256}
classify:{case_version_id}:{taxonomy_version}:{classifier_version}
publish:{publication_version_id}
~~~

新结果先写入 Staging。全部检查通过后，在一个数据库事务中更新当前版本并写入 Outbox Event。

前台只能读取已完成的 Publication Version。

### 8.5 删除和异常

上游删除案例时先标记 tombstoned，不进行物理删除。

以下情况必须阻止发布并保留上一版：

- 解析案例数量超过来源配置的允许下降阈值。
- 图文配对率明显下降。
- 解析结果为零。
- 大量图片突然失效。
- Adapter 出现新 Schema 错误。
- 仓库结构变化导致关键案例消失。

## 9. 图片资产管线

### 9.1 资产角色

图片关联必须明确标记角色：

- output_primary
- output_variant
- input_reference
- subject_reference
- style_reference
- product_reference
- before_edit
- after_edit
- mask
- comparison_left
- comparison_right
- thumbnail

### 9.2 内容寻址存储

允许镜像的图片进入对象存储：

~~~text
s3://prompt-hub-assets/sha256/ab/cd/{full_sha256}
~~~

同一文件只保存一次，来源关系单独保存。

派生规格建议包括：

- original
- detail-1600
- card-720
- thumb-360
- blur-placeholder

原图保留原格式和哈希；展示图可以生成 WebP 或 AVIF，并移除 EXIF。

### 9.3 外部图片

外部图片获取器必须防止：

- SSRF 和私网访问。
- 路径穿越和符号链接逃逸。
- MIME 伪造。
- 超大文件和压缩炸弹。
- 无限重定向。
- SVG 脚本。
- HTML 冒充图片。

只有权利策略允许时才镜像外部图片。link_only 内容默认只保存来源链接和内部元数据，不自动公开远程缩略图。

## 10. 标准化、去重与 Canonical Case

### 10.1 Prompt 标准化

每个 Prompt 可以有以下记录：

- original：上游原文。
- clean：仅移除 Markdown 围栏等格式噪声。
- translation：独立翻译。
- derived_template：抽取变量后的模板。
- analysis：结构化分析。

复制按钮必须清楚区分原文、翻译和派生模板。

### 10.2 参数兼容性

Prompt 中出现的非 OpenAI 参数不得静默删除。例如 Midjourney 的命令行标记应保留在原文，并单独标记 compatibility warning。

### 10.3 去重层级

先处理项目级来源家族，再处理内容级重复。

可使用的证据：

- Prompt 标准化精确哈希。
- 图片文件 SHA-256。
- 原始帖子 URL。
- 作者和上游 Case ID。
- 仓库 Fork 或镜像血缘。
- Prompt 近似度。
- 图片 pHash 或视觉向量。

规则：

- 明确镜像或完全相同的来源案例可以自动归入同一重复组。
- 相同 Prompt 但不同输出图片、模型或输入图，必须保留不同 Generation Example。
- 近似内容只进入审核队列，不凭 Embedding 自动合并。
- Canonical Case 是前台展示分组，不是删除来源证据的工具。

### 10.4 字段级来源

Canonical Case 的标题、主图、Prompt 版本和标签可能来自不同来源或人工编辑，因此需要记录 Field Provenance，避免只保存一组笼统的 provenance。

## 11. 分类与搜索

### 11.1 分类策略

分类应在首批真实存量导入后确定，采用：

~~~text
少量宽泛主类
+ 多维 Facet
+ 上游原始标签
~~~

标签来源分开保存：

- source_tags
- auto_tags
- editor_tags
- blocked_tags
- final_tags

优先级：

~~~text
人工编辑
> 上游明确字段
> 高置信度自动分类
> 低置信度推断
~~~

每次分类保存 taxonomy_version、classifier_version、confidence 和 evidence。

### 11.2 初始 Facet

首轮可从以下维度开始，但必须根据真实数据调整：

- 视觉媒介。
- 主体类型。
- 构图和版式。
- 生成或编辑模式。
- 文字密度与语言。
- 图片方向。
- 风格特征。
- 是否包含参考图。
- 证据完整度。

主类只用于导航，不作为案例永久身份。

### 11.3 搜索

首版使用：

~~~text
PostgreSQL Full Text Search
+ pg_trgm
+ Facet Filter
~~~

只有在真实搜索数据证明有价值后再启用 pgvector 语义搜索和视觉相似搜索。

搜索排序权重不在设计阶段写死，应通过人工相关性样本和用户行为校准。

## 12. 权利、质量与发布门

### 12.1 权利记录粒度

权利信息必须分别记录：

- repository_license
- prompt_rights
- asset_rights
- original_author
- original_post_url
- evidence_url
- reviewed_at
- reviewer

支持以下展示策略：

- mirror_allowed
- attribution_required
- link_only
- internal_only
- blocked

### 12.2 自动发布最低条件

一个 Generation Example 只有同时满足以下条件才能进入自动发布候选：

- 存在完整原始 Prompt。
- 存在 output_primary。
- 图文配对状态为 strong。
- 来源项目、Commit 和文件位置完整。
- 图片文件通过安全和格式检查。
- 权利策略明确允许对应展示方式。
- 未命中阻止发布的内容规则。
- 模型声明及其证据状态能够在前台准确展示，不把未知或混合模型案例误标为 GPT Image 2 实测案例。

任何条件不满足时进入内部库存或人工审核。

### 12.3 下架和纠错

系统必须支持：

- 按单张图片、单条 Prompt、单个案例或整个来源下架。
- 保存投诉、处理结果和时间线。
- 修正错误配对而不破坏历史版本。
- 将错误合并的 Canonical Case 拆分。
- 回滚到上一 Publication Version。

## 13. 网站与管理后台

### 13.1 公共网站

首版只需要完成核心用户流程：

- 图片优先的案例列表。
- 分类和 Facet 筛选。
- 关键词搜索。
- 案例详情。
- 查看完整原始 Prompt。
- 一键复制原始 Prompt。
- 明确区分翻译和派生模板。
- 查看来源项目、原始链接和权利状态。
- 显示参考图需求、模型声明和兼容性警告。

### 13.2 管理后台

后台是内容供应链控制台，至少需要：

- 来源列表和生命周期管理。
- 当前远端 SHA、已入库 SHA 和同步延迟。
- 每次同步的新增、修改、删除和错误统计。
- Adapter 健康指标。
- 模糊图文配对审核。
- 重复案例审核。
- 分类审核。
- 权利状态审核。
- 上游新旧版本差异。
- 发布、回滚和下架。

## 14. 技术选型

首版固定采用以下技术，避免保留多个未决选项：

| 层 | 技术 |
| --- | --- |
| 公共网页与管理后台 | Next.js、TypeScript |
| API 与数据校验 | FastAPI、Pydantic、JSON Schema |
| 采集与解析 Worker | Python |
| 数据库 | PostgreSQL |
| 队列 | Redis、Dramatiq |
| 图片处理 | libvips / pyvips |
| 对象存储 | 本地 MinIO；生产使用 S3 或 Cloudflare R2 |
| Git 操作 | 系统 Git CLI |
| 部署 | Docker Compose |
| 日志 | 结构化日志和 OpenTelemetry |
| 错误监控 | Sentry |

首版不使用 Kubernetes。只有当单机或少量节点无法满足已测量负载时再重新评估。

### 14.1 建议代码结构

~~~text
prompt-hub/
├── apps/
│   ├── web/
│   └── api/
├── ingestion/
│   ├── scheduler/
│   ├── mirror/
│   ├── adapters/
│   ├── pairing/
│   ├── media/
│   └── publication/
├── content/
│   ├── schema/
│   ├── canonicalization/
│   ├── dedupe/
│   ├── taxonomy/
│   └── rights/
├── config/
│   ├── sources/
│   ├── taxonomy/
│   └── rights/
├── fixtures/
│   └── adapters/
├── migrations/
├── tests/
└── docs/
~~~

## 15. 实施路线

### Phase 0：设计和来源冻结

交付物：

- 候选来源审计表。
- sources-v1.yaml。
- Generation Example JSON Schema。
- Adapter 输出契约。
- 权利与发布策略。
- 初始分类词表。
- 验收标准。

这一阶段必须基于仓库当前 Commit 实际测量案例数量和配对率，不能直接复用旧评分结论。

### Phase 1：三类来源纵向打通

已完成并经过统一验证。正式长期来源为 g0dam、JoeSai 与 ConardLi，分别提供 100、50 与 162 个内部 Generation Examples；共 312 个内部案例与 528 个来源文件。

已完成纵向链：

~~~text
镜像
→ 解析
→ 图文配对
→ 图片入库
→ Generation Example
→ 去重
→ 分类
→ 权利门
→ 发布
→ 网页展示
→ Commit 更新同步
~~~

该链已覆盖固定 Commit 镜像、解析、图文配对、内容寻址资产、精确去重、分类、rights/publication fail-closed、只读 API、Next 公共网页和增量同步。当前真实人工 rights approval 为 0，因此真实 public catalog 为 0（0 real public）；312 internal Generation Examples 不等于 312 个已公开案例。实现和本地验证均已完成，但系统尚未部署，也未启用 scheduler 或管理后台。

### Phase 2：接入全部 active 来源

来源接入已经扩展为两种运行语义。当前共有 7 个内部 active 来源：6 个 `continuous` 来源由项目专用 Adapter、固定 Commit、来源审计和独立启停边界约束，包括 Phase 1 的 g0dam、JoeSai、ConardLi，以及 freestylefly、erickkkyt、VigoZhao；Chaos 作为第 7 个 `fixed_history` 来源，只允许固定 Commit 一次性导入，禁止进入增量同步。

当前固定快照合计：

- 3973 个内部 source cases / Prompt records；
- 9310 条 Generation Example 与输出配对关系；
- 9265 个按内容哈希去重后的内部资产对象；
- 12100 个登记来源文件；
- 0 个真实公开案例。

多输出来源不会随机选图：一个 source case 对应一份 Generation document，每个输出分别建立 Generation Example 与强配对证据。首张为 `output_primary`，其余为 `output_secondary`。来源角色不直接决定公开角色；Phase 2.5 已通过独立的案例级审核与 Candidate v2 合同解决选择问题，但 Publication/API/Web v1 仍不读取该合同，也不自动公开任何内容。

### Phase 2.5：案例级人工审核基础

已完成内部 Rights Review Queue 与 Public Case Candidate v2：

- 当前七来源固定库存投影为 3973 个待审 Source Case、9310 个逐输出决定对象；
- 一次审核原子覆盖一个 `source_case_version_id` 的 Prompt 和全部输出；
- 来源 `output_primary/output_secondary` 与人工选择的 `public_primary/public_gallery/hidden` 分离；
- 批次追加后不可修改，支持幂等重放、expected-latest 冲突检查和同案例并发串行化；
- hidden 与所有非 publishable 输出只以无身份的 redacted 占位项计数，不进入公开资产子集；候选 provenance URL 仅允许无查询参数、无凭据、非对象存储主机、非字面或非标准数字 IP 的公开 HTTPS；model label、native id、selector 只允许不带任何 URI scheme 的公开元数据文本，source/prompt/generation/review identity 可保留正常命名空间 ID 但拒绝 URL 与私有存储 scheme，source path 必须保持仓库相对路径并拒绝 Windows 绝对/盘符相对路径及任何 URI scheme；
- 为了在真实权利审核前检查内容质量，提供独立的 loopback-only `/internal-preview`：它读取七个固定 Commit 的 adapter 结果，展示 3973 个真实 Prompt 与 9310 张对应效果图；Chaos 每个案例保留 3 张输出图。所有案例仍明确标记 `review_required`，不写 Publication、不进入 Public API/Web v1，也不构成公开授权；
- reviewer 详情包含全部输入/输出与当前库存 rights evidence；Candidate 只公开严格投影的模型声明，不携带原始参数对象；
- 新 Source Revision 与审核提交共享 source-project 事务锁；新版本形成新的待审 subject，旧审核不会继承，但原成功请求仍可按相同幂等键验证历史批次；
- Candidate v2 目前只用于内部预览，不写入 Publication v1，不被 Public API/Web v1 读取。
- 已新增认证审核 API 与 `/admin/review` 浏览器入口：账号及 scrypt 密码哈希只来自外部运行环境，`viewer/reviewer/admin` 角色分离，HMAC 签名会话、HttpOnly SameSite Cookie、精确 Origin、会话绑定 CSRF 和登录限流共同保护入口；浏览器不能自报 reviewer/reviewed_at，服务端强制绑定当前登录身份与 UTC 时间；
- 审核后台展示完整 Prompt、全部输入/输出、固定来源、已有 rights evidence、历史批次与 Candidate v2 预览；每张图必须逐项选择 asset rights、display policy 和 public display role，不提供 approve-all，也没有 Publication 激活入口。

六个持续源的 1513/1930 审核基线与 Chaos 的 2460/7380 固定历史导入已分别完成隔离真实装配，累计口径闭合为 3973/9310；Erick/Chaos 多输出、事务回滚、并发、Schema 和脱敏边界保持不变。旧 Content/API/Web 回归继续保持 0 个真实公开案例，当前仍没有录入任何真实人工批准。

### Phase 2.6：大规模来源双通道准入

`source-expansion-admission-v2`（`TASK-0021`）保留 ecomimagelab、HiAPIAI、imagineVid 的 probation/空批次结论；其后 `source-expansion-admission-v3`（`TASK-0022`）完成两个大规模 full candidates、一个家族 comparator 和一个 exclusion control 的固定版本审计，并把持续同步与一次性固定历史明确分成两个不同通道：

- `Goku-OpenLab/gpt-image-2-prompts-datasets`：固定 Hugging Face revision `c4e79e9e11b3e754ec64f6400c7f94de6a5f103d`，精确模型范围 16759 条；经过来源、资产、风险、YouMind/Atlas 家族和当前库存 exact 去重后保留 2513 条。其维护与 LFS 权威满足 continuous 基础条件，但确定性质量样本仅 47/60 通过，仍有身份文档、成人/未成年人、品牌、名人、第三方 IP、参考图依赖和低信息案例漏入，因此状态为 `blocked`，不进入 Adapter handoff。
- `ChaosRealmsAI/gpt-image-2-gallery`：固定 Git revision `5296db8c996e38776c83a0bc8c64f848dcd512b3`，3798 个 raw cases、11559 个本地图片资产；过滤风险、参考图依赖、缺失资产和当前 exact 重复后保留 2460 条。60/60 准入质量样本通过，12/12 排除原因样本成立，完整 immutable snapshot 闭合，因此状态为 `fixed_history_ready`。
- `YouMind-OpenLab/gpt-image-2-prompts-search`：只作为 Goku/Atlas 聚合家族的 Prompt SHA-256、参考图和归因边界 comparator。固定版本含 23283 条分类记录、33779 个 CMS 图片引用（21444 个唯一 URL），15714 条声明需要参考图，但作者归因和原帖链接暴露均为 0；远程图片只能记为 `observation_only`，状态为 `comparator_only`，不得进入批次。
- `tigerowo/awesome-gpt-image-2-prompts`：只保留 EvoLink backup/家族映射排除证据，状态为 `excluded`，没有建立完整准入账。

本轮唯一 `adapter_ready_batch` 项 Chaos 已完成后续接入：v2 operational registry/audit、`chaos_meta_three_webp_v1` Adapter、完整 exclusions 绑定、不可变提取包、一次性私有库存导入、Canonical 成员和七来源内部预览均已闭合。固定 Commit `5296db8c996e38776c83a0bc8c64f848dcd512b3` 产出 2460 个 Source Case、7380 个输出、7380 个互异资产对象和 9840 个来源文件；它仍保持 `sync_eligible=false`、`one_shot_import_only=true`，增量同步入口会显式拒绝该来源。所有 Prompt/图片仍为 `review_required`、`auto_publish=false`，没有审核批次、Publication Version 或公开条目，真实 public catalog 仍为 0。

### Phase 3：公开网站完善

- 完成案例列表、详情、搜索、筛选和复制。
- 完成来源页和权利说明。
- 管理后台审核流程已完成；真实权利决定仍必须由被授权审核人依据证据录入，系统不会代替法律判断。
- 只发布满足公开条件的案例。

### Phase 4：长期运行

- 启用定时更新检测。
- 建立同步失败、数量突降和图片失效告警。
- 建立来源新增、暂停、退休和替换流程。
- 根据真实数据调整分类和搜索。
- 定期复核来源权利与可用性。

## 16. 验收标准

### 16.1 来源与镜像

- 所有 active 来源都能成功镜像并记录稳定 Repository ID。
- 每个已处理 Commit 都有不可变 Source Revision。
- 上游改名、Force Push 或删除分支不会破坏已处理历史。

### 16.2 解析与数据正确性

- 同一来源、Commit 和 Adapter 版本重复执行不会产生重复记录。
- 每个公开案例都有完整原始 Prompt 和至少一张 output_primary。
- 每张公开输出图都关联一个明确 Generation Example。
- ambiguous 配对自动公开数量为零。
- Adapter 测试结果与固定夹具一致。
- 未知模型或参数不会被自动补造。

### 16.3 更新与恢复

- 来源 Commit 变化后能正确识别新增、修改、删除和重命名。
- 解析失败不会删除或覆盖上一版已发布内容。
- 超过来源阈值的案例数量下降会自动阻断发布。
- 重复同步任务可以安全重试。
- Publication Version 可以回滚。

### 16.4 去重与来源

- 镜像仓库不会重复创建前台案例。
- 相同 Prompt 的不同生成结果不会丢失各自 Generation Example。
- 每个公开字段和资产都可以追溯到来源记录。
- 近似重复不会仅凭向量相似度自动合并。

### 16.5 权利与发布

- 每个公开 Prompt 和图片都有明确展示策略。
- rights 状态为 unknown、internal_only 或 blocked 的图片不会被公开镜像。
- 系统支持按资产、案例和来源下架。
- 派生翻译和模板不会冒充原始 Prompt。

### 16.6 用户功能

- 用户可以按图片浏览案例。
- 用户可以查看并准确复制原始 Prompt。
- 用户能够识别翻译、模板和原文的区别。
- 用户可以查看来源、参考图要求和兼容性警告。
- 搜索和分类结果只包含当前发布版本。

## 17. 主要风险

| 风险 | 处理方式 |
| --- | --- |
| README 或数据结构变化 | 专用 Adapter、固定夹具、数量下降阻断 |
| Prompt 与图片错配 | Generation Example、配对等级、模糊案例人工审核 |
| 镜像和重复内容 | Source Family、内容哈希、重复审核 |
| 外部图片失效 | 本地资产策略、失效检测、来源链接 |
| 权利不清 | 内部库存与公开目录分离、细粒度 rights |
| 上游删除或重写历史 | Git 镜像和内部快照引用 |
| 自动分类错误 | 标签来源分层、版本化、人工覆盖 |
| 存储增长 | 内容寻址、图片去重、按需获取历史 Blob |
| 项目长期停更 | paused/retired 生命周期，不影响已有案例 |

## 18. 当前立即执行的下一步

`source-expansion-admission-v3`（`TASK-0022`）的 Chaos handoff 已实施完成。Goku 因 13/60 质量样本失败继续 blocked；YouMind 只作 comparator，tigerowo 维持 excluded。`source-expansion-admission-v2`（`TASK-0021`）的 ecomimagelab、HiAPIAI 和 imagineVid 也仍保持原 probation 结论，没有被静默升级。

当前合同为 7 个内部 active 来源（6 continuous + 1 fixed_history）、3973 internal Source Cases、9310 outputs、12100 source files、9265 deduplicated asset objects、0 real public。Chaos 已进入私有库存和内部预览，但没有进入 scheduler、Publication 或公开页面。

来源扩展线后续规则：

1. Chaos 固定历史源只能重复验证同一准入权威和同一幂等导入，不得改成 continuous、跟随 HEAD 或进入 scheduler。
2. Goku 若要再次竞争 continuous，必须先增强其来源专用风险/完整性过滤并重新执行独立质量抽样；当前 2513 条 filtered 子集不能直接接入。
3. YouMind 与 tigerowo 不编写 Adapter；前者继续作为家族 comparator，后者继续作为 exclusion control。TASK-0021 的三个 probation 来源若要挽救，仍需各自建立独立的高质量子集重审任务。
4. 新来源继续执行固定版本、完整资产权威、强图文配对、内容质量抽样和 exact 家族去重，不得为了扩大数量降低质量门槛。

产品与发布线下一步：

1. 被授权审核人可通过现有认证后台按明确证据录入首批真实决定；在没有真实证据前继续保持 public catalog 为 0，不制造示例批准。
2. Public API/Web v2 已显式读取独立 current，支持多图、稳定公开 URL、搜索筛选、完整 Prompt、来源权利说明；认证后台也已提供 admin-only 构建、激活、回滚和下架/恢复入口，v1 current 保持独立。
3. 当前主线只剩生产部署、scheduler、告警、来源生命周期与长期运营流程；没有真实批准时继续保持 0 public。

七来源内部接入、案例级审核、认证管理、Publication/API/Web v2、下架/恢复和发布运营入口已经完成；当前主线转为生产部署、长期调度、告警、来源生命周期和真实公开运营。

## 附录 A：候选来源审计字段

每个项目的审计结果至少包含：

~~~text
source_id
repository_url
repository_id
verified_commit_sha
default_branch
source_family_role

declared_case_count
observed_case_count
valid_case_count
exact_prompt_count
paired_output_count
pair_rate
broken_asset_count
duplicate_estimate

content_structure
adapter_strategy
adapter_complexity
lfs_usage
external_asset_usage
model_scope

last_substantive_content_update
maintenance_evidence

repository_license
prompt_rights_status
asset_rights_status
original_source_coverage

recommended_status
ingestion_policy
publication_policy
review_notes
~~~

## 附录 B：已有调研的使用方式

现有调研文件可以作为候选来源发现材料，但不能直接作为生产来源注册表：

- gpt-image-2-two-track-research-2026-08-01.md
- gpt-image-2-two-track-research-2026-08-01.xlsx

正式来源必须重新以固定 Commit、实际解析结果和权利证据完成准入审核。
